= form_tag(login_path, :id => "login-form") do
  %fieldset
    .field= text_field_tag(:email, nil, :placeholder => "E-mail")
    .field= password_field_tag(:password, nil, :placeholder => "Password")

  .actions
    = submit_tag("Log in")
    = link_to("...or register", register_path)
